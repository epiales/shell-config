#!/bin/sh

# --foramt = ( across, commas, horizontal, long, single-column, verbose, vertical )
alias a="ls -A"
alias l="ls -AFhl"
alias cwd="basename `pwd`"

alias reload-profile="source ${HOME}/.zshrc"
alias edit-profile="atom $HOME/.zshrc"
