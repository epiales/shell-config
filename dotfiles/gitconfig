[user]
	name = Logan Brown
	email = logan.brown1223@gmail.com
	github = epiales
[color]
	ui = true
[alias]
	amend = commit --amend --no-edit
	backup = "!git checkout -b backup-$(date +\"%Y-%m-%d_%H-%M-%S\")-$(git symbolic-ref --short HEAD); git checkout -; git lg -4"
	br = branch
	ck = checkout
	ckb = checkout -b
	cm = commit
	cmm = commit -m
	date = "!git commit --amend --date=\"$(date)\""
	ds = diff --staged -C
	fa = fetch --all
	fu = fetch upstream
	l = !git lg -10
	l2 = !git lg -20
	la = !git lg --all
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	prune-all = !git remote | xargs -n 1 git remote prune $1
	quick = !git add -A; git commit -m \"quick commit\"
	st = status
	standup = "!git lg --since yesterday --author \"$(git config user.name)\" --all"
	undo = reset --soft HEAD^
[core]
	pager = less -FXRS -x4
	editor = atom -w
	excludesfile = /Users/logan.brown/.gitignore_global
	filemode = false
	autocrlf = input
[difftool]
	prompt = false
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
[mergetool "sourcetree"]
	cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
	trustExitCode = true
[push]
	default = simple
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
[column]
	ui = auto
